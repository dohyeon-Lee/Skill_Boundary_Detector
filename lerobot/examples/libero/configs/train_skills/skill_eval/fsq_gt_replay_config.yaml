
# One FSQ model or a list of models to evaluate together. Each entry pairs its
# real run folder (run_name) with the label shown in HTML (model_name).
# With several entries, output_name becomes the comparison parent and each run writes to
# output_name/<run_name>; afterwards build the tabbed comparison report:
#   python src/fsq_gt_replay_report.py --compare outputs/fsq_gt_replay/<output_name>/*/
# 네이밍 규칙 (2026-08 정리): constLR(기본 LR)·말미 _oneshot 마커·bsq의 joint(항상 joint)는
# 폴더명에서 제거. 구조 라벨은 입출력 모달리티로 표기: pos_pos(스플라인→ctrl 복원),
# pos_act(스플라인→action rollout), act_act(action 시퀀스→action rollout).
# LR 태그는 전 run 필수: e-4대=LR4, e-5대=LR5, e-6대=LR6 (enc≠dec인 옛 LRdown 계열은 LenTok/에 보관).
# encoder 입력 모드 태그도 전 run 필수: raw | zero | opt(optimal) — LR 태그 바로 뒤 슬롯.
# 길이토큰 세대 run(구 noLenTok 미표기 시절)은 LenTok/에 보관 — noLenTok 라벨 폐지(무토큰이 기본).
# loss 구조 태그 전 run 필수: ReconOnly(entropy 항 없음) | withEntH(entropy 항 포함; BSQ는 항상 withEntH).


# output_name: "recon_only_routing_2layer"
# fsq_eval_models:
#   - {"model_name": "route_OFF", "run_name": "zero_recon_only__contrastiveON_routeOFF_loss"}
#   - {"model_name": "route_ON", "run_name": "zero_recon_only__contrastiveON_routeON_loss_"}

# output_name: "contrastive_vs_js_2layer"
# fsq_eval_models:
#   - {"model_name": "cont_route_OFF", "run_name": "zero_recon_only__contrastiveON_routeOFF_loss"}
#   - {"model_name": "cont_route_ON", "run_name": "zero_recon_only__contrastiveON_routeON_loss_"}
#   - {"model_name": "js_route_OFF", "run_name": "zero_recon_only__jsON_routeOFF_loss"}
#   - {"model_name": "js_route_ON", "run_name": "zero_recon_only__jsON_routeON_loss"}

# output_name: "2layer_vs_1layer"
# fsq_eval_models:
#   - {"model_name": "2layer_route_OFF", "run_name": "zero_recon_only__contrastiveON_routeOFF_loss"}
#   - {"model_name": "2layer_route_ON", "run_name": "zero_recon_only__contrastiveON_routeON_loss_"}
#   - {"model_name": "1layer_route_OFF", "run_name": "zero_recon_only__contrastiveON_routeOFF_loss__layer1"}
#   - {"model_name": "1layer_route_ON", "run_name": "zero_recon_only__contrastiveON_routeON_loss__layer1"}


# output_name: "2layer_full"
# fsq_eval_models:
#   - {"model_name": "cont_route_OFF", "run_name": "zero_recon_only__contrastiveON_routeOFF_loss"}
#   - {"model_name": "cont_route_ON", "run_name": "zero_recon_only__contrastiveON_routeON_loss_"}
#   - {"model_name": "js_route_OFF", "run_name": "zero_recon_only__jsON_routeOFF_loss"}
#   - {"model_name": "js_route_ON", "run_name": "zero_recon_only__jsON_routeON_loss"}
#   - {"model_name": "none_route_OFF", "run_name": "zero_recon_only__pairOFF_routeOFF_loss"}
#   - {"model_name": "none_route_ON", "run_name": "zero_recon_only__pairOFF_routeON_loss"}

# output_name: "routeON_action"
# fsq_eval_models:
#   - {"model_name": "cont", "run_name": "zero_recon_only__contrastiveON_routeON_loss_"}
#   - {"model_name": "js_zero", "run_name": "zero_recon_only__jsON_routeON_loss"}
#   - {"model_name": "js_action", "run_name": "action_recon_only__jsON_routeON_loss"}
#   - {"model_name": "none_zero", "run_name": "zero_recon_only__pairOFF_routeON_loss"}
#   - {"model_name": "none_action", "run_name": "action_recon_only__pairOFF_routeON_loss"}

# output_name: "normalized_action"
# fsq_eval_models:
#   - {"model_name": "action_js", "run_name": "action_recon_only__jsON_routeON_loss"}
#   - {"model_name": "norm_action_js", "run_name": "norm_action1_recon_only__jsON_routeON_loss"}
#   - {"model_name": "norm01_action_js", "run_name": "norm_action01_recon_only__jsON_routeON_loss"}
#   - {"model_name": "norm01_action_none", "run_name": "norm_action01_recon_only__pairOFF_routeON_loss"}

# output_name: "gripper_effect"
# fsq_eval_models:
#   - {"model_name": "js_norm_action", "run_name": "norm_action1_recon_only__jsON_routeON_loss"}
#   - {"model_name": "js_norm_action01", "run_name": "norm_action01_recon_only__jsON_routeON_loss"}
#   - {"model_name": "js_zero", "run_name": "zero_recon_only__jsON_routeON_loss"}
#   - {"model_name": "js_zero01", "run_name": "zero01_recon_only__jsON_routeON_loss"}
#   - {"model_name": "none_zero", "run_name": "zero_recon_only__pairOFF_routeON_loss"}
#   - {"model_name": "none_zero01", "run_name": "zero01_recon_only__pairOFF_routeON_loss"}

# output_name: "terminator_effect3"
# fsq_eval_models:
#   - {"model_name": "norm_action", "run_name": "norm_action01_recon_only__jsON_routeON_loss"}
#   - {"model_name": "norm_action_RESNET", "run_name": "norm_action01_recon_termRES__jsON_routeON_loss__layer2_base"}
#   - {"model_name": "norm_action_RESNET_01", "run_name": "norm_action01_recon_termRES__jsON_routeON_loss__term01"}
#   - {"model_name": "norm_action_RESNET_unfrez_01", "run_name": "norm_action01_recon_termRES__jsON_routeON_loss__vis_unfreeze_term01"}

# output_name: "raw_proprio_conditioned"
# fsq_eval_models:
#   - {"model_name": "raw_js_proprio", "run_name": "raw_recon_only__jsON_routeON_loss__inital_proprio_conditioned"}
#   - {"model_name": "raw_none", "run_name": "raw_recon_only__pairOFF_routeON_loss"}
#   - {"model_name": "raw_none_proprio", "run_name": "raw_recon_only__pairOFF_routeON_loss__inital_proprio_conditioned"}

# output_name: "norm_action_01"
# fsq_eval_models:
#   - {"model_name": "cont",  "run_name": "norm_action01_recon_only__contrastiveON_routeON_loss"}
#   - {"model_name": "js",    "run_name": "norm_action01_recon_only__jsON_routeON_loss"}
#   - {"model_name": "none",  "run_name": "norm_action01_recon_only__pairOFF_routeON_loss"}

output_name: "norm_action_series"
fsq_eval_models:
  - {"model_name": "contrastive",  "run_name": "norm_action01_recon_only__contrastiveON_routeON_loss"}
  - {"model_name": "JS", "run_name": "norm_action01_recon_only__jsON_routeON_loss"}
  - {"model_name": "JS + frez_termination", "run_name": "norm_action01_recon_termRES__jsON_routeON_loss__term01"}
  - {"model_name": "JS + unfrez_termination", "run_name": "norm_action01_recon_termRES__jsON_routeON_loss__vis_unfreeze_term01"}

fsq_eval_checkpoint: [50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 1000, 1500, 2000]

# How episodes get their task id.
#   exact   : from the rendered episode-exact map (skillvla_dataset/<ds>/eval_init_states.npz).
#             target_task must then be a real LIBERO suite, and episodes whose scene
#             is outside that suite are dropped.
#   dataset : from the dataset's own meta/tasks.parquet.  Covers every episode even
#             when the dataset draws scenes from several suites (langgap_56 spans
#             langgap_ext + libero_spatial + libero_object + libero_goal + libero_10),
#             needs neither the exact map nor libero_original_dataset, and makes
#             target_task a report label only.  No original-HDF5 demo provenance.
episode_source: exact        # exact | dataset

target_task: libero_90 # langgap_56 | libero_90
# List of task ids, or "all" for every task the dataset can serve. Ids without
# enough episodes in the dataset are skipped with a warning.
# The list below picks one task per libero_90 scene (20 scenes; task 51 is
# absent from the filtered dataset).
task_ids: [0, 3, 4, 5, 6, 11, 18, 22, 28, 33, 35, 38, 40, 46, 50, 55, 60, 65, 69, 73, 77, 81, 86]
# task_ids: all
episodes_per_task: 20
episode_selection: first       # first | random
episode_ids: []                # optional exact global episode IDs
seed: 42

# eval_max_concurrent caps how many replay tasks run at once; workers_per_checkpoint
# splits one checkpoint's occurrences across that many jobs. Images come straight
# from the dataset videos (no simulator) and the replay holds NO GPU, so this is a
# Replay concurrency throttle. With inherited train QOS, each slot also reserves
# one GPU even though the replay computation itself remains CPU-only.
eval_max_concurrent: 2   # one replay job per listed FSQ run
# "all" makes one CPU allocation replay every available checkpoint for a run.
# The process keeps decoded frames in RAM, so later checkpoints neither re-import
# the stack nor decode the same episode frames again.
checkpoints_per_job: all
workers_per_checkpoint: 1
resume: true
              # FSQ_VS_BSQ

slurm:
  # gres/latents_time cover the GPU prepass that encodes each checkpoint's skill
  # latents -- the only GPU work in this pipeline, submitted as ONE job per run.
  gres: gpu:1
  latents_time: "01:00:00"
  # Replay computation only uses CPU. When replay_qos is blank, however, it
  # inherits train placement and reserves gpu:1 to satisfy GPU-QOS rules.
  # With an explicit replay_qos, blank replay_gres means true CPU-only.
  replay_gres: ""
  # Blank -> global_config.yaml train_partition/train_qos + one GPU reservation.
  # For CPU-only, explicitly set dell_cpu/cpu_qos and leave replay_gres blank.
  replay_partition: "" # "dell_cpu"
  replay_qos: "" # "cpu_qos"
  # dell_cpu is only 4 nodes (52 CPUs / 450G each), so a smaller footprint fits
  # many more replay tasks at once: 8 CPU/64G allows 6 per node, 4 CPU/16G
  # allows 13. The replay itself just decodes two frames per occurrence.
  replay_cpus: 4
  replay_memory: 16G
  cpus: 8
  memory: 64G
  time: "01:00:00"
