# Single-policy, episode-exact multi-terminator skill evaluation.
#
# For every selected training episode, each FSQ skill occurrence is evaluated
# independently from its exact simulator state. Each HTML entry plays one
# synchronized horizontal comparison:
#   GT | exact | exact with different noise | early | late

outputs_root: outputs_filtered

# model:
#   previous: false
#   model_dir: bs32_libero_90_full_firsthalf_scene_FSQ333_norm_action01_recon_only__jsON_routeON_loss_2000_std_pt_episodemean_100p_snap10_pmax15_halfnormal_arch0_skillendmask_visunfreeze
#   checkpoint: "030000"
#   label: norm_action
#   terminator_source: external # own (Stage-1) | original (source FSQ) | external

# external_skill_model:
#   variant: state_image
#   group: skillVLA_terminator
#   model_dir: bs32_FSQ333_norm_action01_recon_only__jsON_routeON_loss_2000_std_pt_episodemean_100p_snap10_pmax15_halfnormal_libero_90_full_firsthalf_scene_terminator_resnet_unfrez
#   checkpoint: "050000"

# terminator_models:
#   - label: RESNET_unfreeze
#     variant: state_image
#     model_dir: bs32_FSQ333_norm_action01_recon_only__jsON_routeON_loss_2000_std_pt_episodemean_100p_snap10_pmax15_halfnormal_libero_90_full_firsthalf_scene_terminator_resnet_unfrez
#     checkpoint: "050000"
#   - label: DINO_unfreeze
#     variant: state_image
#     model_dir: bs32_FSQ333_norm_action01_recon_only__jsON_routeON_loss_2000_std_pt_episodemean_100p_snap10_pmax15_halfnormal_libero_90_full_firsthalf_scene_terminator_DINO_unfrez
#     checkpoint: "050000"

# output_name: "norm_action_visunfreeze"

model:
  previous: false
  model_dir: bs32_libero_90_full_firsthalf_scene_FSQ3333_termRES_contrastive_1750_pt_arch0_skillendmask_visunfreeze
  checkpoint: "180000"
  label: norm_action
  terminator_source: original # own (Stage-1) | original (source FSQ) | external

# external_skill_model:
#   variant: state_image
#   group: skillVLA_terminator
#   model_dir: bs32_FSQ333_zero01_recon_only__jsON_routeON_loss_2000_std_pt_episodemean_100p_snap10_pmax15_halfnormal_libero_90_full_firsthalf_scene_terminator_DINO_unfrez
#   checkpoint: "050000"

terminator_models:
  - label: DINO_freeze
    variant: state_image
    model_dir: bs32_FSQ3333_termRES_contrastive_1750_pt_libero_90_full_full_terminator
    checkpoint: "100000"

output_name: "cotrained_vs_seperate_successX"

resume: true

target_task: libero_90
task_ids: [0, 3]

# Episode-exact is mandatory for this evaluator and cannot fall back to a
# language-matched or standard LIBERO init state.
episode_exact: true
episodes_per_task: 3
episode_selection: first     # first | random
episode_ids: []              # optional exact global dataset episode ids
seed: 42

# Policy branches start at frame_start-offset, frame_start, frame_start+offset.
time_shift_offset: 15
n_action_steps: 5

# Upper bound on physical GPUs. Multiple independent occurrence workers share
# each GPU, and unused GPUs are never requested.
eval_num_gpus: 10
eval_max_workers_per_gpu: 4

terminator:
  end_mode: termination               # termination | progress | or | and
  # Each termination bar freezes at its first crossing of this threshold.
  end_threshold: 0.5
  progress_threshold: 0.95
  # Per-occurrence rollout budget = ceil(GT skill length × this scale).
  # Use max_skill_length instead only when a fixed budget is intentionally needed.
  max_skill_length_scale: 2 #1.50
  # true preserves the fixed-replanning behavior: a detected boundary stops
  # only after the already-generated action chunk has been consumed.
  finish_action_chunk_on_end: true

video:
  frame_stride: 2
  fps: 10

# Explicit for auditability. The resolver checks that every scene/demo named by
# eval_init_states.npz exists here.
original_dataset_dir: libero_original_dataset/libero_90

slurm: {gres: "gpu:1", cpus: 8, memory: 64G, time: "00:30:00"}
